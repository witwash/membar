name: recipes_repository
description: A repository that exposes recipes, libraries, and the active library to the app.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  recipes_api:
    path: ../recipes_api

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^11.0.0
